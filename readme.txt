1. In this project we will read the data from the html form

2. The data which is submitted through html form is store in a dictionary  object,
   that dictionary object will given to a corresponding Http-request Method
   Attribute of HttpRequest class object .

 3. data through HTML form => Httprequest method class object => Argument to the
    parameter of view function or view class method

 4. Syntax :- variable = request.GET["key"]
              variable = request.POST["key"]